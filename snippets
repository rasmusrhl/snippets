

EE : ∃x 
AA : ∀x 
ne : ≠
in : ∈ 
nin : ∉ 
^^ : ∧ 
vv : ∨
cup : ∪ 
cap : ∩ 
sube : ⊆ 
imp : →
iff : ↔
m : × 
int : ∫ 
sum : Σ 
prod : ∏ 
ASCIImath compatible above here.

triang :  △ 
angle : ∠ 
alpha : α 
betal : β 
prop : ∝ 
pi : π 
delta : Δ 
gammal : γ 
theta : θ 
int : ∫ 
div : ÷ 


# Vim
- Matematiske symbolers placering i vim's yank-registre
- Kun a-z bruges. A-Z (uppercase) er ikke selvstændige registre men bruges til at addere tekst.
 
Kopier:
- definér register (gåseøjne + register) 
- yank (y)
- område
	space/l (karakteren under cursoren)
	iw (indre ord ([a-zA-Z]
- `"ayl`
Indsæt:
- gåseøjne + register + paste

- a `forall`: ∀
- e `exists` : ∃
- g `gange`: ×
- i `in`: ∈ 
- l `leftrightarrow`: ↔
- n `negation`: ⌐
- o `og`: ∧   ???
- r `rightarrow`: →
- s `sum`: ∑
